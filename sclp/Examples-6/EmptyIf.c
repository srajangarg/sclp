void main();
main()
{
    int a, b;
	float f, g;
    f = -1e0;
    g = 1e1;
    // if (!(a < b)) { 

    // }
	if (f <= g || !(f <= g) || a > b) {
        print(f+g);
        print("\n");
    }
    print(f);
    print("\n");
    return;
}
